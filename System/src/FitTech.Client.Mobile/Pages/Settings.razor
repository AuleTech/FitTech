@page "/settings"
@inject NavigationManager NavigationManager

<div class="min-h-screen flex flex-col ">
    <!-- Content container -->
    <div class="flex-grow flex flex-col justify-between p-4 max-w-sm w-full mx-auto">

        <!-- Top Section -->
        <div>
            <h2 class="text-center text-xl font-bold border-b border-white pb-2 mb-4">Ajustes</h2>

            <div class="space-y-4 text-sm">
                <div>
                    <span class="font-semibold">Nombre</span>
                    <p class="text-gray-300">Admin FitTech</p>
                </div>
                <div>
                    <span class="font-semibold">Email</span>
                    <p class="text-gray-300">admin@fittech.es</p>
                </div>
                <div>
                    <span class="font-semibold">Entrenador</span>
                    <p class="text-gray-300">Admin Trainer</p>
                </div>
            </div>
        </div>

        <!-- Bottom Section: Buttons + Menu -->
        <div class="space-y-4 pt-6">
            <button 
                @onclick="Logout"
                class="w-full py-2 rounded-md border border-red-400 text-red-300 font-semibold hover:bg-red-500 hover:text-white active:scale-95 transition-transform">
                Cerrar Sesión
            </button>

            <button
                class="w-full py-2 rounded-md border border-red-500 bg-red-800 bg-opacity-30 text-red-200 font-semibold hover:bg-red-600 hover:text-white active:scale-95 transition-transform">
                Eliminar cuenta
            </button>
        </div>
    </div>
</div>

@code {
    public void Logout()
    {
        NavigationManager.NavigateTo("/logout");        
    }
}